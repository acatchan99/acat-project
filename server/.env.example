# ── Railway（API + CMS 后台）Dashboard → Variables ──

NODE_ENV=production
ADMIN_PASSWORD=请改成强密码
JWT_SECRET=请改成随机长字符串

# Railway 自动注入 PORT，无需手动设置

# 本服务对外域名（Railway → Settings → Networking → 生成域名）
# 用于 CMS 上传图片返回完整 URL，Vercel 官网才能显示后台上传的图
PUBLIC_BASE_URL=https://你的项目.up.railway.app

# 可选：限制 CORS（逗号分隔）。留空则允许所有来源
# ALLOWED_ORIGINS=https://acat-project.vercel.app,https://你的自定义域名.com

# 注意：Railway 不要设置 VITE_API_URL（后台 /admin 走同域 /api）
# VITE_API_URL 只在 Vercel 前端环境变量里设置
